# RiotJS Vite Setup

### Quickstart

Install npm packages:
```bash
npm install
```

Start development server:
```bash
npm run dev
```

Build the front for production into a `dist` directory.
```bash
npm run build
```

